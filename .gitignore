# ═══════════════════════════════════════════════════════════════
#  NUNCA PUBLICAR — datos privados
#  El repositorio es PÚBLICO. Todo lo de aquí abajo se queda en
#  local. Si alguna vez editas este archivo, no toques este bloque.
# ═══════════════════════════════════════════════════════════════
PRIVADO/
*.local
*.local.*
.env
.env.*
secretos*
credenciales*
clave*.txt
*.key
*.pem

# Respaldos de cada versión antes de modificarla. Son copias locales:
# git ya conserva todo el historial, así que versionarlas solo engordaría
# el repositorio público con duplicados. Viven solo en la máquina.
respaldos/

# La aplicación no tiene dependencias. Esto es solo de las pruebas.
pruebas/node_modules/
pruebas/package-lock.json

# Capturas que genera pruebas/interfaz.js
pruebas/capturas/

# Sistema
Thumbs.db
desktop.ini
.DS_Store
